#!/usr/bin/env python3
"""Print only the non-secret Message automation trigger prefix."""

from pathlib import Path
import sys


REPO = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(REPO / "src"))

from iphone_cli.config import command_prefix  # noqa: E402


print(command_prefix())
